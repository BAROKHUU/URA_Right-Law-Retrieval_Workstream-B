# Data

- `enriched/`: copied from the supplied Workstream A enhanced corpus. These are the retrieval inputs.
- `benchmarks/`: benchmark/query files from Workstream C should go here later.

The retrieval code does not modify Workstream A data. Generated indexes and experiment results are written only under `artifacts/`.
